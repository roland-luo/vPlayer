{
  "$schema": "../node_modules/@tauri-apps/cli/schema.json",
  "identifier": "default",
  "description": "Default capabilities for the vPlayer app",
  "windows": ["main"],
  "permissions": [
    "core:window:allow-set-fullscreen",
    "core:default",
    "shell:allow-open"
  ]
}
